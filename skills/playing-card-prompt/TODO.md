# TODO

- [x] Card back: support generating a prompt for the deck's card back design
- [x] Joker card: support the Joker as a card type/rank in the wizard
- [x] Special card: support custom/special cards outside the standard rank set
- [x] Title: support adding a title/text overlay to the card. 
- [x] Split: control whether the card is split into mirrored halves (and how — horizontal edge-to-edge vs. angled around the figures) or shows full-body figures without a split
- [x] Seamless design: support a connecting/seamless design across the card (or across the deck)
- [ ] Config mode: add a "layers overview" sub-step near Step 6 that prints the full 7-layer x 3-group (court/pip/ace) matrix for the active profile (via `manage_config.py show`/`profile show <name>`), and lets the user edit it with free-text targeting (e.g. "highlights включи везде", "figure у pip тоже") translated into `manage_config.py set layers.<layer>.<group> true/false` calls — acts as a fast path before/instead of the guided 6.1-6.4 questions
- [x] Figure types: add a per-card `figure_type` wizard step (character / building / animal / custom) that controls which sub-settings apply. Gate Face Style and `character_framing` on `figure_type == character` only. Add two deck-wide persistent fields: `figure_scale` (full bleed / inscribed in frame / small centered — applies to all figure types) and `character_framing` (head / bust / waist-up / 7/8 / full body — character only, replaces current `figure_proportion`). Update STYLE_BLOCK assembly to apply these gates in the sборщик rather than in pattern files.
- [x] Fix shipped `config.json` breaking first-run wizard: the file is git-tracked and packaged as-is by `scripts/package-skill.sh` (only `.DS_Store`/`*.swp`/`config.json.bak*` are excluded), so a fresh install already has a fully populated `default` profile. Per `SKILL.md`'s Generate-mode logic, "Config exists" short-circuits straight to per-card steps, so the intended "no valid config found → run full 11-step Config wizard" path never triggers for a new install — only after an explicit `--reset`. Decide and implement a fix: either stop committing/packaging `config.json` (let `manage_config.py`'s in-code `DEFAULTS` be the only source until the user actually saves), or otherwise mark the shipped file so Generate-mode treats it as "no valid config" until the user has gone through the wizard once.
- [ ] Title: convert from a flat deck-wide toggle (`title.enabled`) into a `layers.title.<group>` layer with alias presets (new `assets/title/*.md` files) controlling position/font/styling per group — e.g. Joker: caption below the figure; court cards: name running along the sides — while keeping the title text itself per-card and never saved. Also fix doc inconsistencies found in audit: add `title.enabled` to the "Persistent" field list and the per-card title text to the "Per-card" field list in `references/CONFIG.md` (both currently omitted from those two enumerations, though the field reference table documents them); add a `title` row/mention to `references/STYLE-COMPONENTS.md` #5 Composition/rhythm (currently `WIZARD-STEP-MAP.md` maps Title overlay to #5 but STYLE-COMPONENTS.md's own text for #5 never mentions title).
- [ ] Card back: convert `back_purpose`/`back_design`/`back_pattern`/`back_palette`/`back_symmetry` from deck-wide persistent config fields (Steps B1–B6 in `WIZARD-BACK.md`, skipped once set) to per-card ephemeral wizard steps — the style is assembled fresh through the wizard steps each time, like other per-card prompts, and is never written to `config.json`.
- [x] Style-components doc gap: `references/STYLE-COMPONENTS.md` has no row/mention for the Special/Prospect card group (`rank = Special`, Steps S1-S5 in `WIZARD-STEP-MAP.md`, "Special group defaults" in `references/CONFIG.md`) even though `back` gets an explicit callout at the top of the file. Add a row or note covering Special/Prospect as a card-group component, or fold it into the "Open gaps" section if intentionally out of scope.
- [ ] Unify `layers.<layer>.<group>` value semantics to one `false|true|alias|custom` contract: currently 4 different resolution models exist (bool+addition for background/decor/ornaments/highlights/frame/technique; bool-gate-vs-full-text spec/implementation gap for mood; strict enum with no free-text slot, and a `true→false` footgun on pip/ace, for figure; alias+custom-text with diverging `"true"` resolution between split and seamless). See local GSD todo `unify-layers-false-true-alias-custom-syntax` for full analysis and proposed reconciliation.
- [ ] Reduce wizard structural complexity: (1) consolidate the scattered per-`(layer, group)` default tables (currently restated inline across Steps 3/6/8b/8c/8f and again in REFERENCE.md's Defaults table) into one canonical location with pointers elsewhere; (2) simplify the repeated multi-condition skip-chains on Steps 8a-8f/T (each opens with 2-4 flat prose conjunctions, easy to drop one silently on edit); (3) consider resequencing the `structure: illustration` setting earlier in the wizard (e.g. near Step 1 or folded into Step 5) since it currently gates behavior re-derived independently at Steps 8f, T, and 3 assembly steps downstream. See local GSD todo `simplify-skill-defaults-skip-chains-structure-order` for full analysis.
- [ ] `references/REFERENCE.md`'s `## structure setting (full vs illustration)` section is currently prose (the "exactly five changes" list plus a separate "everything else is unaffected" paragraph). Reformat it as a table — one row per layer/element (Index, Frame, opening line, Negative list, Title, Seamless, Background/Decor/Ornaments/Highlights, Technique/Finish, Figure, Mood, Center motif) with `full` vs `illustration` columns — so the two modes are scannable at a glance instead of requiring a full read-through.
- [ ] Local install copies dev-only files into installed skills: `scripts/package-skill.sh` (used by `scripts/install-local.sh`) only excludes `.DS_Store`/`*.swp`/`config.json`/`config.json.bak*` from the tar, so `.gitignore`, `CHANGELOG.md`, and `TODO.md` end up shipped into `~/.claude/skills/<name>/` on every local install. `scripts/package-skill-claudeai.sh` already excludes `TODO.md` (but not `.gitignore`/`CHANGELOG.md`) for the Claude.ai zip path. Decide which of these are genuinely dev-only (likely all three) and exclude them consistently across both packaging scripts.
