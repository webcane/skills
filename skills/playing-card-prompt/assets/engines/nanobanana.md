# Engine: NanoBanana (default)

No adaptation needed — this is the base format the templates in
`references/REFERENCE.md` already produce.

1. **Negative handling** — inline, as the trailing `[NEGATIVE_LIST]` line
   (`no watermark, no cannon, ...`).
2. **Aspect ratio syntax** — inline lead-in phrase, `[ASPECT_RATIO] aspect ratio,`.
3. **Extra parameters** — none.
